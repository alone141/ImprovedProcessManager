# Journal PID colors — plan

**Goal:** Color Service-tab journal lines by journald `_PID`.

**Tasks:** (1) JSON parse + tests in `systemd_logs.py` (2) `QTextEdit` paint + worker emit structured lines (3) README.

See spec `docs/superpowers/specs/2026-08-14-journal-pid-colors-design.md`.
